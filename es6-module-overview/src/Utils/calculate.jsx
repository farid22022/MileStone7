const add =(first , second) => {
    return first+second;
}

const multiply = (first , second) =>{
    return first*second;
}

export {add , multiply as mult}

// export default add;

// export default add;
// const calculator ={add:add}
// export default calculator